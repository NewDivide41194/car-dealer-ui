import { JSX } from "react";

const StyledButton = (): JSX.Element => {
  return (
    <div className="p-4">
      <button color="blue">Click Me</button>
    </div>
  );
};

export default StyledButton;
